{
  "requiredIntegrations": [
    {
      "type": "aws",
      "title": "AWS",
      "category": "CLOUD"
    }
  ],
  "acceptanceCriteria": [
    {
      "value": 0,
      "condition": "GREATER THAN",
      "query": "FIND * as a WHERE a._integrationType = \"aws\" RETURN count(a)"
    }
  ],
  "supportedUseCase": "Shows AWS IAM Groups, Users, Roles, and important access policy assignments."
}
