{
  "requiredIntegrations": [
    {
      "type": "aws",
      "title": "AWS",
      "category": "CLOUD"
    }
  ],
  "acceptanceCriteria": [
    {
      "value": 0,
      "condition": "GREATER THAN",
      "query": "FIND * as a WHERE a._type =\"aws_account\" RETURN count(a)"
    }
  ],
  "supportedUseCase": "This dashboard requires the J1 AWS integration with one or more AWS accounts."
}
